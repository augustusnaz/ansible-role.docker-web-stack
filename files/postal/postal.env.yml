web:
  # The host that the management interface will be available on
  host: ${HOSTNAME}.${DOMAIN}
  # The protocol that requests to the management interface should happen on
  protocol: https

fast_server:
  # This can be enabled to enable click & open tracking on emails. It is disabled by
  # default as it requires a separate static IP address on your server.
  enabled: false
  bind_address:

general:
  # This can be changed to allow messages to be sent from multiple IP addresses
  use_ip_pools: false

main_db:
  # Specify the connection details for your MySQL database
  host: ${DB_HOST}
  username: ${DB_USERNAME}
  password: ${DB_PASSWORD}
  database: ${DB_DATABASE}

message_db:
  # Specify the connection details for your MySQL server that will be house the
  # message databases for mail servers.
  host: ${DB_HOST}
  username: ${DB_USERNAME}
  password: ${DB_PASSWORD}
  prefix: ${DB_PREFIX}

rabbitmq:
  # Specify the connection details for your RabbitMQ server.
  host: 127.0.0.1
  username: postal
  password: p0stalpassw0rd
  vhost: /postal

dns:
  # Specifies the DNS record that you have configured. Refer to the documentation at
  # https://github.com/atech/postal/wiki/Domains-&-DNS-Configuration for further
  # information about these.
  mx_records:
    - mx.${HOSTNAME}.${DOMAIN}
  smtp_server_hostname: ${HOSTNAME}.${DOMAIN}
  spf_include: spf.${HOSTNAME}.${DOMAIN}
  return_path: rp.${HOSTNAME}.${DOMAIN}
  route_domain: routes.${HOSTNAME}.${DOMAIN}
  track_domain: track.${HOSTNAME}.${DOMAIN}

smtp:
  # Specify an SMTP server that can be used to send messages from the Postal management
  # system to users. You can configure this to use a Postal mail server once the
  # your installation has been set up.
  host: 127.0.0.1
  port: 2525
  username: # Complete when Postal is running and you can
  password: # generate the credentials within the interface.
  from_name: Postal
  from_address: postal@${DOMAIN}

rails:
  # This is generated automatically by the config initialization. It should be a random
  # string unique to your installation.
  secret_key: 9f48b9f1c5c59e3d8ee8716d5ded47e8d482678360b54bc574381ac31143195ef8cf4e7eb1520fbab215efa8d12edf9475c6ba5bd2635244e2f2ab77c45092a3bd84d5cdbeeaba0dead16b556fe1319fdca9ed347219b9c9c14a1922fc2ab881b6af57882f$
